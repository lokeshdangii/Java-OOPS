/*
    Usually every Java class is considered as encapsulated class, when we say that it is tightly encapsulated, it means every variable present inside the class is private.
    
    if and only if every variable present inside the class is private then it is said to be tightly encapsulated class.

    Definition:- A class is said to be tightly encapsulated if and only if every variable of that class declared as private whether the variable has getter and setter methods are not , and whether these methods declared as public or not, these checkings are not required to perform.

 */

